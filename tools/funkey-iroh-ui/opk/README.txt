Iroh is installed by the optional FunKey-OS `-iroh` firmware profile.

The Settings entry handles service state, pairing cards, per-peer post-game
save synchronization, received-save review, and multiplayer readiness. The
actual Iroh endpoint remains a headless service.
