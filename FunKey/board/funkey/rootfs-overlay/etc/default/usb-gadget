# "auto" enables USB networking when /mnt/usbnet exists; "always" forces it.
USB_NETWORK=auto
# Keep the traditional shared-disk function in the composite USB profile.
USB_MASS_STORAGE=1
# Dormant unless a derived hardware-debug image enables it.
USB_DEBUG=0
USB_DEBUG_HOLD_AFTER_FLASH=0
