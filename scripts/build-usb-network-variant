#!/bin/bash
set -euo pipefail
repo=$(cd "$(dirname "$0")/.." && pwd)
exec "$repo/scripts/build-usb-variant" \
    "${1:?usage: build-usb-network-variant VERSION}" \
    network-only scripts/usb-gadget-network-only
